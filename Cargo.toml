[workspace]
members = ["lsp-server"]
exclude = ["extension"]
resolver = "2"
# Note: extension/ is excluded because it targets wasm32-wasip1
# and has different compilation requirements. Zed compiles it separately.
